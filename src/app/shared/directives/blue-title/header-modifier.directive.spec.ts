import { HeaderModifierDirective } from './header-modifier.directive';

describe('HeaderModifierDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderModifierDirective();
    expect(directive).toBeTruthy();
  });
});
