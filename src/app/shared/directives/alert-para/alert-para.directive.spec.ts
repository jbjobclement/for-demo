import { AlertParaDirective } from "./alert-para.directive";

describe('AlertParaDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertParaDirective();
    expect(directive).toBeTruthy();
  });
});
