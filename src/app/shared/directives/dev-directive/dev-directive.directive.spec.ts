import { DevDirectiveDirective } from './dev-directive.directive';

describe('DevDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DevDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
