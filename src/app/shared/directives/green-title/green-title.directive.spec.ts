import { GreenTitleDirective } from './green-title.directive';

describe('GreenTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new GreenTitleDirective();
    expect(directive).toBeTruthy();
  });
});
