import { DxTextBoxDirective } from './dx-text-box.directive';

describe('DxTextBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new DxTextBoxDirective();
    expect(directive).toBeTruthy();
  });
});
