import { DirectiveWithServiceDirectiveTest } from './directive-with-service-test.directive';

describe('DirectiveWithServiceDirectiveTest', () => {
  it('should create an instance', () => {
    const directive = new DirectiveWithServiceDirectiveTest();
    expect(directive).toBeTruthy();
  });
});
