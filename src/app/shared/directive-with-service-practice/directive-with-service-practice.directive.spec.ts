import { DirectiveWithServicePracticeDirective } from './directive-with-service-practice.directive';

describe('DirectiveWithServicePracticeDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveWithServicePracticeDirective();
    expect(directive).toBeTruthy();
  });
});
